/* =========================================================
   College Corridor — compare.js
   Dynamic College Comparison Logic
   ========================================================= */

(function() {
  'use strict';

  // Compare Tool initialization if needed
  document.addEventListener('DOMContentLoaded', function() {
    // Shared modal triggers and interactive helpers for comparison
  });
})();
